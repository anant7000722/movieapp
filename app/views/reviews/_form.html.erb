<%= form_for([@movies, @movies.reviews.build]) do |form| %>
<% if @movies.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(review.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% reviews.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <% if user_signed_in? %>
    <div class = "col-md-2" >
      <img style = "width: 100px;height: 100px;" src="<%= @img %>" class="img-thumbnail title-show" />
    </div>
      <%= form.label :comment %>
      <%= form.text_area :comment, id: :review_comment %>

    <% else %>
      <%= form.label :comment %>
      <%= form.text_area :comment, id: :review_comment %>
    <% end %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
